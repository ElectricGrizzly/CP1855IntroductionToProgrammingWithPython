"""User inputs for Prime Number Checker program."""

def get_user_number(prompt: str) -> int:
    """Returns the users' input integer value."""
    return int(input(prompt))

def get_user_character(prompt: str) -> str:
    """Return the users' input character value."""
    return input(prompt)